/* Support Page Logic — FAQ accordion and contact form */
document.addEventListener('DOMContentLoaded', () => { initFAQ(); initContactForm(); });
function initFAQ() { document.querySelectorAll('.faq-question').forEach(btn => { btn.addEventListener('click', () => { const item = btn.parentElement; const isActive = item.classList.contains('active'); document.querySelectorAll('.faq-item').forEach(i => i.classList.remove('active')); if (!isActive) item.classList.add('active'); }); }); }
function initContactForm() { document.getElementById('contactForm')?.addEventListener('submit', async e => { e.preventDefault(); try { await api('/support/contact',{method:'POST',body:JSON.stringify({name:e.target.querySelector('input[type="text"]').value,email:e.target.querySelector('input[type="email"]').value,subject:e.target.querySelector('select').value,message:e.target.querySelector('textarea').value})}); } catch {} showToast("Message sent! We'll get back to you within 24 hours. 🌿",'success'); e.target.reset(); }); }
